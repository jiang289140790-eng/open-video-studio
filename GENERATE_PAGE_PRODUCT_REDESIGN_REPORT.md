# AI 图片生成页产品化重构报告

日期：2026-07-27  
页面：`https://jiang289140790-eng.github.io/open-video-studio/generate.html`  
范围：仅 AI 图片生成页及其直接前端依赖  
最终验收状态：`PARTIALLY_ACCEPTED`

## 1. 修改前问题

- 页面更接近内部测试工作台，重复说明、步骤卡片和技术信息过多。
- 上传参考图入口与 A01 纯文生图能力不一致，容易让用户误以为上传图片会参与生成。
- 提示词下方长期展示多条完整示例，信息密度过高，也不适合作为公共页面默认内容。
- 比例、数量、积分和提交区域分散，用户难以快速理解主要生成路径。
- 结果区的空、排队、生成、成功、失败状态层级不够统一。
- 完成状态曾过度信任任务状态；当输出 URL 缺失或图片无法加载时，仍可能进入不完整的成功界面。
- 最近任务信息过重，挤占生成页主要操作空间。
- 页脚体积偏大，主内容与页脚之间存在不必要留白。

修改前基线详见：

- `GENERATE_PAGE_BEFORE_BASELINE.md`

## 2. 删除和收敛的重复内容

- 删除独立的“当前为纯文生图”大型说明卡片。
- 删除参考图上传控件，改为标题区简短提示“当前版本暂不支持参考图”。
- 删除输入框下长期展示的完整示例提示词。
- 删除“成人主题示例”标题及公共页面中的长期敏感示例。
- 删除独立积分确认大卡片，积分信息收敛到提交按钮上方一行。
- 删除成功结果区重复的第二组操作按钮。
- 删除不适用于本页的保存按钮，改为明确提示结果已进入“我的创作”。
- 最近任务收敛为最多 3 个紧凑缩略卡。
- 大型页脚替换为工具页简化页脚。

## 3. 新页面信息架构

### 标题区

- `AI 图片生成`
- 副标题：`输入文字描述，快速生成高质量图片`
- 标签：`Qwen Image`、`文生图`
- 能力提示：`当前版本暂不支持参考图`
- 工作流可用状态

### 左侧“创建图片”

1. 图片描述输入框
2. 优化描述、灵感示例、清空
3. 图片比例
4. 生成数量
5. 高级设置
6. 预计积分 / 访客免费体验
7. 开始生成按钮

### 右侧“生成结果”

- `empty`
- `queued / running`
- `succeeded`
- `failed / output_failed`
- 最近 3 个任务

桌面端采用约 40% / 60% 两栏；920px 以下切换为单列。

## 4. 修改文件

- `apps/web/generate.html`
- `apps/web/generation-workspace.js`
- `apps/web/styles.css`
- `GENERATE_PAGE_BEFORE_BASELINE.md`
- `GENERATE_PAGE_PRODUCT_REDESIGN_REPORT.md`
- `reports/screenshots/generate-page-product-desktop-online.png`
- `reports/screenshots/generate-page-product-mobile-online.png`

## 5. 保留的真实功能

本次没有修改后端接口、A01 工作流、Supabase 表结构或积分服务。

保留：

- A01 真实任务提交入口
- 访客体验会话
- 登录用户会话与余额展示
- 任务轮询和刷新恢复
- 成功结果显示
- 下载
- 再次生成
- 转视频
- 我的创作入口
- 积分不足跳转

未加入假的 `setTimeout` 成功结果，也未伪造进度百分比。

## 6. 灵感示例

“灵感示例”改为弹窗/移动端底部抽屉，包含 6 类、共 12 个中性模板：

- 写真人像
- 时尚摄影
- 电影氛围
- 动漫角色
- 产品摄影
- 风景与建筑

选择模板后只填入输入框，用户仍可继续修改。弹窗支持关闭、分类切换和键盘焦点。

## 7. 参数白名单

前端只展示当前 A01 已支持的比例映射：

| 用户选项 | 后端尺寸 |
|---|---:|
| 1:1 | 1024 × 1024 |
| 16:9 | 1280 × 720 |
| 9:16 | 720 × 1280 |

生成数量固定为当前工作流支持的 1 张。

Seed 支持：

- 随机
- 自定义整数
- 范围：0–4294967295

页面未展示工作流节点、ComfyUI 内部参数名或可任意注入的节点参数。

## 8. 积分与按钮状态

登录用户：

- 显示 `预计消耗：X积分 · 当前余额：Y`
- 余额不足时显示缺口和“获取积分”
- 按钮禁用并显示“余额不足”

访客：

- 显示 `本次免费体验 · 登录后可保存作品`
- 输入满足条件后按钮显示“免费生成”

提交状态：

- 正在提交
- 等待队列
- 模型准备
- 生成中
- 上传结果

## 9. 结果区状态

### empty

- 居中图标
- “生成的图片会显示在这里”
- 简短操作引导

### queued / running

- 真实任务状态文案
- Loading 动画
- 不显示伪造的精确百分比

### succeeded

- 按实际比例完整显示图片
- 图片可点击查看大图
- 主操作：下载
- 次操作：再次生成、转视频
- 其他操作进入更多菜单

### failed / output_failed

- 显示用户可理解的错误摘要
- 不直接展示 PostgREST、Edge Function 或 ComfyUI 完整错误堆栈
- 提供重新生成或查看帮助
- 保留退款核验提示

## 10. 结果图片加载失败的根因与处理

旧逻辑主要依赖任务的 `completed` 状态，输出 URL 缺失、过期或不可读时，没有在展示层可靠降级。

本次增加：

1. 完成任务必须存在输出 URL。
2. 图片必须触发真实 `load` 事件后才能显示成功操作。
3. 图片加载失败时自动刷新一次任务数据，尝试获取更新后的地址。
4. 刷新后仍无有效输出时进入 `output_failed`，不再显示假成功。
5. 最近任务媒体加载失败时显示统一占位，不显示浏览器破图图标。

仍受后端约束：前端不能自行续签私有 Storage URL；若后端没有返回新地址，只能友好提示并等待服务端恢复。

## 11. 最近生成

- 仅显示最近 3 个任务。
- 显示缩略图、状态和时间。
- 失败任务使用统一失败占位。
- 详细列表统一进入“我的创作”。

## 12. 桌面端验收

验证尺寸：

- 1920px
- 1440px
- 1366px
- 768px

结果：

- 两栏顶部对齐。
- 1440px 最大内容宽度生效。
- 1366 × 768 下核心输入和提交按钮可见。
- 页面无横向滚动。
- 右侧结果区和最近生成保持独立层级。

线上桌面截图：

- `reports/screenshots/generate-page-product-desktop-online.png`

## 13. 390px 手机端验收

结果：

- 参数、按钮、状态、结果、最近生成按单列排列。
- 比例按钮完整显示。
- 页面实测 `innerWidth = 390`、`scrollWidth = 390`。
- 无页面级横向滚动。
- Header 收敛为 Logo、积分入口和菜单。
- 灵感示例使用底部抽屉。

线上手机截图：

- `reports/screenshots/generate-page-product-mobile-online.png`

## 14. 自动检查结果

| 检查 | 结果 |
|---|---|
| `npm run typecheck` | 通过 |
| `npm run lint` | 未执行成功：项目未定义 `lint` script |
| `npm run build` | 通过 |
| `npm test` | 75 项中 59 项通过、16 项失败 |
| `git diff --check` | 通过 |

现有测试失败集中在跨页面静态断言、支付入口、SEO 路由和其他消费者页面，不是本页脚本运行错误。本任务没有为通过测试而修改范围外页面。

构建仍有既有警告：

- 部分传统脚本不是 ES module。
- 部分其他页面的 home assets 在构建时保持运行时解析。

## 15. Git 提交

页面代码提交：

- `37bb3931c72dabab028ec6f9b08bce97887ee3b8`
- `Productize AI image generation page`

状态：`PUSHED`

## 16. GitHub Pages 部署

GitHub Actions 运行：

- Run ID：`30237517088`
- 构建：成功
- 部署：成功

页面代码部署 commit：

- `37bb3931c72dabab028ec6f9b08bce97887ee3b8`

状态：`DEPLOYED`

线上已确认标题、标签、纯文生图说明、工作流状态、访客免费体验和按钮状态均来自新版本。

状态：`ONLINE_VERIFIED`

## 17. 截图证据

- 桌面端：`reports/screenshots/generate-page-product-desktop-online.png`
- 手机端：`reports/screenshots/generate-page-product-mobile-online.png`

截图来自 GitHub Pages 线上页面，不是本地页面。

## 18. 仍未解决的问题

1. 本任务没有发起新的真实 A01 生成，以避免在 UI 范围任务中产生额外生成费用；因此没有用本次提交重新验证一条全新的线上生成结果。
2. `npm run lint` 不可用，因为项目没有配置 lint script。
3. 全项目测试仍有 16 个范围外失败，需要在对应页面和支付/SEO任务中处理。
4. 如果 Supabase 返回的图片 URL 过期，前端只能刷新任务数据；真正的 URL 续签仍需要现有服务端链路支持。
5. Edge Function、积分退款和“我的创作”数据库写入未在本次 UI 任务中改动，也未重新做付费端到端验收。

## 19. 最终结论

- UI 重构：已部署并完成线上桌面/手机验证。
- 真实链路保护：前端调用接口和任务恢复逻辑未被替换。
- 新鲜线上生成：本任务未执行。

最终状态：`PARTIALLY_ACCEPTED`

要达到完整 `ACCEPTED`，下一步只需在当前线上版本执行一次受控的 A01 真生成回归，并核验下载、“我的创作”、积分和 Storage 记录；不需要再次改版。
