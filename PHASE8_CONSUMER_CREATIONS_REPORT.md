# Phase 8 — Consumer Creations Center 报告

## 1. 任务范围

本阶段仅改造消费者前台“我的作品”页面及其直接依赖的数据操作：

- 消费者作品中心首屏
- 搜索、类型、状态、收藏与可见性筛选
- 生成任务与作品的合并展示
- 作品预览、下载、再次生成、转视频、收藏、分享、删除、公开/私密
- 未登录与无作品空状态
- 高级资产能力入口
- 可撤销、可过期的只读分享链接

未修改生成工作流、积分扣减、支付、模型配置或 AI Marketing Studio 运营后台。

## 2. 现状审计与根因

### 页面结构

原 `my-creations.html` 同时包含：

- 360px 高宣传 Banner
- 四块运营统计卡
- 作品区
- 独立“最近生成”任务区
- “下一步”运营引导区

这使首屏更像资产运营后台，搜索、筛选和真实作品被推到首屏以下。

### 数据重复

原页面分别渲染 `media_assets` 与 `generation_jobs`，没有通过：

- `generation_jobs.result_asset_id`
- `media_assets.generation_job_id`

进行去重。已完成任务与其输出作品可能同时显示。

### 操作缺失

数据库已有：

- `media_assets.is_favorite`
- `media_assets.visibility_status`
- `media_assets.deleted_at`
- `share_links.revoked_at`

但消费者页面没有收藏、隐私切换、删除、撤销分享的真实服务端操作。

### 分享安全

原 `create-share-link` 会将 `media_assets.visibility_status` 改为 `public`，分享链接没有过期时间；`share_links` 还存在匿名公共读取策略。私密作品无法保持私密，链接也不能由消费者撤销。

### 生产结构只读核对

已只读检查 Supabase 项目 `open-video-studio`：

- `generation_jobs` 已包含任务状态、错误、退款关联所需字段。
- `media_assets` 已包含生成任务关联、收藏、可见性与软删除字段。
- `share_links` 已包含撤销字段，但生产库当前尚无 `expires_at`。
- 生产 `ai` Edge Function 当前处于 ACTIVE；本阶段本地修改尚未部署。

未执行生产数据库迁移、Edge Function 部署或数据写入。

## 3. 修改文件

### `apps/web/my-creations.html`

- 删除巨大宣传 Banner、统计卡、重复历史区与消费者首屏无关的资产管理入口。
- 首屏直接展示搜索、状态/类型筛选、可见性筛选、作品与任务网格。
- 增加预览弹窗和分享管理弹窗。
- 增加独立“进入创作者工作台”入口，隐藏角色、提示词、工作流、审核、发布、日历和分析等高级能力。

### `apps/web/app.js`

- 将 `generation_jobs` 与 `media_assets` 合并为同一消费者网格。
- queued/processing 等任务排在最前，failed 次之，completed 作品随后。
- 通过 `result_asset_id` 与 `generation_job_id` 去重。
- 映射创建时间、工具、效果、错误与退款信息。
- 新增收藏、公开/私密、软删除、分享创建、分享撤销的持久化操作。
- 图片作品支持“转成视频”，复用现有生成恢复数据。
- 未登录显示登录与浏览工具；已登录无作品显示三个真实工具入口。
- 移除“我的作品”页额外的通用登录门槛，避免重复空状态。
- 分享页改为通过服务端令牌解析私密资产，不再直接公开查询作品表。

### `apps/web/styles.css`

- 建立消费者作品网格、任务状态卡、媒体占位、空状态、对话框和响应式布局。
- 桌面端四列，逐级降为三列、两列和手机单列。
- 筛选栏在小屏横向滚动，不造成整页横向滚动。
- 媒体失败显示“预览准备中”，不显示纯黑或破图。

### `supabase/functions/ai/index.ts`

新增：

- `get-shared-asset`
- `update-media-asset`
- `delete-media-asset`
- `revoke-share-link`

调整：

- 分享默认 7 天过期，最大 30 天。
- 分享不再自动把私密作品改为公开。
- 分享读取返回短期签名 URL，不返回 Storage 写权限。
- 删除作品会软删除并撤销所有活动分享链接。
- 所有修改操作校验当前用户对作品或分享链接的所有权。

### `supabase/migrations/202607260001_consumer_creation_shares.sql`

- 为 `share_links` 增加 `expires_at`。
- 增加活动分享链接索引。
- 移除匿名直接读取 `share_links` 的策略。
- 保留所有者与运营角色读取策略。

## 4. 已实现功能

| 功能 | 状态 |
|---|---|
| 首屏搜索与筛选 | 已完成 |
| 全部/图片/视频/生成中/失败/已收藏 | 已完成 |
| 公开/私密二级筛选 | 已完成 |
| queued/processing 优先显示 | 已完成 |
| failed 原因与退款状态 | 已完成 |
| 任务与作品去重 | 已完成 |
| 刷新恢复远程状态 | 已完成 |
| 图片/视频缩略图 | 已完成 |
| 无预览媒体占位 | 已完成 |
| 预览 | 已完成 |
| 下载 | 已完成 |
| 再次生成 | 已完成 |
| 图片转视频 | 已完成 |
| 收藏 | 已完成 |
| 公开/私密切换 | 已完成 |
| 删除与分享撤销联动 | 已完成 |
| 创建、复制、撤销分享链接 | 已完成 |
| 分享过期时间 | 本地实现，待迁移部署 |
| 未登录空状态 | 已完成 |
| 已登录无作品空状态 | 已完成 |
| 高级工作台入口 | 已完成 |

## 5. 验证结果

### 自动验证

- `npm run typecheck`：通过
- `npm run build`：通过
- `git diff --check`：通过
- 页面结构断言：通过
  - 无巨大作品 Banner
  - 六个消费者筛选完整
  - 任务/作品去重逻辑存在
  - 收藏、分享、撤销、删除服务端动作存在
  - 未登录和无作品空状态完整

构建仍报告项目原有的非 module 脚本与首页素材路径警告，本阶段没有新增构建错误。

### 浏览器验证

- 本地页面可正常打开。
- Header、Sidebar 与全局操作各只有一组。
- 未登录只显示一个消费者空状态，不再叠加通用登录门槛。
- 搜索、筛选、刷新、高级工作台入口均可访问。
- 1280×720 浏览器首屏无横向滚动。
- 手机断点已按 820px 与 540px 实现：单列作品、横向筛选、全宽操作、对话框安全宽度。

验收截图：

- `reports/screenshots/phase8-consumer-creations-desktop.png`

## 6. 未完成项与原因

### 生产分享过期与消费者写操作尚未上线

原因：

- 按项目规则，本任务没有自动执行生产数据库迁移或 Edge Function 部署。
- 生产库只读核对确认 `share_links.expires_at` 尚不存在。

上线前需要：

1. 审核并应用 `202607260001_consumer_creation_shares.sql`。
2. 部署更新后的 `ai` Edge Function。
3. 使用真实测试用户验证收藏、可见性、分享、撤销和删除。

### 真实账户完整状态矩阵未写入测试

未在生产执行真实写操作，因此以下场景已完成 UI 与代码路径，但需要部署后验证：

- 已登录无作品
- 图片与视频作品
- queued/processing/failed 任务
- 收藏持久化
- 分享创建、到期和撤销
- 删除后分享失效

### 手机真实截图

当前内置浏览器验收会话固定为 1280×720，无法切换设备视口；手机布局已通过响应式样式和无溢出规则检查，但仍建议部署前在 390×844 与 430×932 真机视口补充截图。

## 7. 下一任务依赖

- 应用分享过期迁移。
- 部署 `ai` Edge Function。
- 准备一个包含图片、视频、生成中和失败任务的测试账户。
- 在测试环境完成写操作与手机真机回归后再发布生产。
