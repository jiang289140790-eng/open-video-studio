# Phase 3 — HiFun-style Consumer Homepage

## 1. 任务范围

本阶段只重构消费者首页，不删除现有路由，不新增模型，不修改生成 API、积分扣减、支付、Supabase 数据库结构或 AI Marketing Studio 后台逻辑。

首页内容已按以下顺序整理：

1. Hero 生成入口
2. 热门效果
3. 图片工具
4. 视频效果
5. 三步使用说明
6. 真实作品展示
7. 隐私与可靠性
8. 积分套餐入口
9. FAQ
10. 消费者 Footer

## 2. 现状审计与根因

### 2.1 原首页问题

- 首页由多个介绍模块、工具入口和旧内容块堆叠，首屏不能直接完成“上传—选效果—查看积分—生成”的核心决策。
- 页面混用了消费者语言和内部工作流/模型语言。
- 热门效果、作品展示和媒体预览缺少可靠的真实数据来源。
- 公共脚本会在页面尾部再次注入旧 Footer，导致新首页 Footer 与旧 Footer 重复。
- 旧 Footer 还包含不属于本阶段消费者 Footer 的入口和旧联系方式。

### 2.2 数据审计结论

- Supabase `tools` 中有 3 条已发布工具记录。
- Supabase `workflows` 中有 3 条已发布工作流记录，但输入、输出 schema 为空。
- 本地 `workflow-map.json` 中相关工作流状态仍为 `requires_server_config`。
- `homepage_sections` 为空。
- 当前没有可核验的公开作品或独立效果预览媒体。
- 因此，本阶段没有把任何效果伪装成 `active`，也没有用无关图片替代真实预览。

### 2.3 可用状态规则

首页只把同时满足以下条件的效果视为可生成：

- 效果状态为 `active`
- 存在 `workflow_id`
- 工作流状态为 `active`

热门效果和视频效果还必须存在真实可用的预览媒体。当前数据未满足条件，所以页面显示明确的“正在完成验证 / 即将上线”空状态，生成按钮保持禁用。

## 3. 修改文件

| 文件 | 修改内容 |
|---|---|
| `apps/web/app.html` | 重建首页语义结构，按指定顺序组织 Hero、效果、工具、说明、作品、可信度、价格、FAQ 和 Footer |
| `apps/web/homepage.js` | 新增首页交互、真实可用状态过滤、上传校验、预览、登录检查和生成入口控制 |
| `apps/web/styles.css` | 新增首页桌面端和移动端响应式样式 |
| `apps/web/app.js` | 防止首页已有消费者 Footer 时再次注入旧全局 Footer |
| `scripts/copy-web-assets.mjs` | 将 `homepage.js` 纳入生产构建复制清单 |

## 4. 已实现内容

### 4.1 Hero 生成入口

- 左侧包含主标题、价值说明、图片/视频效果切换、上传入口、效果选择、预计积分、生成按钮和隐私说明。
- 右侧用于真实媒体预览；无真实媒体时显示非误导性的“已验证效果预览”状态。
- 支持 JPG、PNG、WebP，最大 20 MB。
- 未登录用户可以先选择效果和上传素材。
- 只有点击生成时才检查登录状态。
- 只有真实 `active workflow` 才会启用生成按钮。
- 1440×900 下生成按钮完整出现在首屏。

### 4.2 热门效果与视频效果

- 热门效果只接受 `active workflow + 真实预览 + featured/HOT/NEW`。
- 视频效果只接受 `active workflow + 真实预览`。
- 当前无满足条件的数据，因此使用紧凑、可解释的验证中状态，不输出黑色空卡。
- 页面没有批量加载视频，避免影响首屏性能。

### 4.3 图片工具

仅保留消费者可理解的名称：

- 图片编辑器
- AI 换脸
- 服装变换
- 姿势生成
- 图片组合

工具未具备真实 active workflow 时标记“即将上线”，但现有路由保持可达。

### 4.4 如何使用

首页使用三步短流程：

1. 上传图片
2. 选择效果
3. 下载结果

### 4.5 真实作品与可信度

- 没有伪造作品、用户数量、满意度或评分。
- 没有公开授权作品时，显示“暂无可公开展示的真实作品”。
- 可信度区域只展示可核验规则入口：一次性购买、失败任务处理、文件与隐私、任务历史。

### 4.6 Footer

消费者 Footer 只保留：

- 产品：首页、全部效果、图片工具、视频工具、我的作品
- 帮助：Blog、价格、推荐好友、Terms、Privacy、Cookie
- 联系：support、business

旧全局 Footer 已停止在首页重复注入，运营后台入口不再出现在首页 Footer。

## 5. 验证结果

### 5.1 自动检查

| 检查 | 结果 |
|---|---|
| `npm run typecheck` | 通过 |
| `npm run build` | 通过 |
| `git diff --check` | 通过 |
| 浏览器控制台 error/warning | 0 |
| 破损图片 | 0 |
| 首页视频自动加载 | 0 |
| 新旧 Footer 重复 | 已修复，消费者 Footer 1 个、旧 Footer 0 个 |

构建仍输出项目原有的经典脚本打包提示和旧 `home-assets` 运行时解析提示；本阶段首页没有使用这些旧媒体路径，生产复制脚本会保留需要的经典脚本。

### 5.2 响应式验收

| 尺寸 | 横向滚动 | 核心 CTA | 结果 |
|---|---:|---:|---|
| 1440×900 | 无 | 首屏完整可见 | 通过 |
| 1920×1080 | 无 | 首屏完整可见 | 通过 |
| 390×844 | 无 | 首屏完整可见 | 通过 |
| 430×932 | 无 | 首屏完整可见 | 通过 |

### 5.3 页面结构验收

- 首页指定的 10 个内容区顺序正确。
- Header、Sidebar、全局操作和客服组件未重复渲染。
- 首页只有一套消费者 Footer。
- 图片/视频效果切换状态正确。
- 无可用 workflow 时 CTA 明确禁用，并显示原因。
- 生产构建中首页、效果、工具、作品、价格、免费积分、Blog、Terms、Privacy、Cookie 等入口文件均存在。
- 浏览器环境不支持自动选择本地文件，因此没有把真实文件选择写成已通过；上传类型、大小、状态更新和隐私逻辑已由代码路径与页面状态检查覆盖，仍建议上线前手动执行一次真实文件上传。

## 6. 截图

- `docs/screenshots/phase3-consumer-homepage/homepage-1440.png`
- `docs/screenshots/phase3-consumer-homepage/homepage-390.png`
- `docs/screenshots/phase3-consumer-homepage/homepage-430.png`
- `docs/screenshots/phase3-consumer-homepage/homepage-full.png`

## 7. 未完成项

### 7.1 真实可生成效果

状态：UI 与状态控制已完成，API 数据未满足开放条件。

依赖：

- 为目标工作流补齐输入 schema 和输出 schema。
- 将本地工作流状态改为真实验证后的 `active`。
- 进行至少一次真实端到端运行。
- 配置对应积分、耗时和错误处理。

### 7.2 真实预览与公开作品

状态：容器与空状态已完成，真实数据暂缺。

依赖：

- 上传经过授权的独立效果预览媒体。
- 为作品增加公开授权和审核状态。
- 只查询通过审核、允许公开的作品。

### 7.3 上传端到端人工验证

浏览器自动化环境不支持本地文件选择。生产发布前需要人工选择一张 JPG/PNG/WebP，确认文件名、预览、效果选择和登录门槛连续工作。

## 8. 风险与下一任务依赖

1. 不应仅把数据库工作流状态改成 `active`；必须先完成输入输出 schema、真实运行和结果解析验证。
2. 没有真实预览时继续保持当前“验证中”状态，不要复用无关封面。
3. 构建中的旧 `home-assets` 提示来自其他页面的历史样式，建议单独清理，避免与本阶段范围混合。
4. 只读 Supabase 审计同时发现共享项目中有 45 张营销/后台相关表未启用 RLS。该问题不属于 Phase 3，未自动修改；在公开生产访问前应安排独立的数据库安全审计与修复任务。

## 9. 下一阶段建议顺序

1. 选定 1 个最成熟工作流完成真实 active 验证。
2. 补齐该效果的独立预览、积分、预计时间和输入/输出 schema。
3. 用这一条真实效果完成首页上传—登录—生成—结果—作品记录的端到端验收。
4. 验证通过后，再逐个开放热门效果和视频效果。
