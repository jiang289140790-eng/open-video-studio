# Phase 7 — Image-to-Video Consumer Flow

## 1. 范围与结论

本阶段只完善消费者前台的图片转视频流程、统一任务状态、动态积分展示、结果操作和任务可靠性。没有新增模型、没有伪造生成结果、没有修改 Supabase 表结构，也没有执行生产部署或真实扣费任务。

当前结论：

- 【UI 已完成】图片转视频统一工作台、视觉效果弹层、条件化参数、动态积分、任务阶段、结果操作。
- 【交互已完成】效果筛选与选择、价格联动、积分不足入口、重复提交锁、刷新恢复、取消任务、失败/退款/超时提示。
- 【服务端已完成，待部署】幂等任务 ID、原子任务领取、服务端动态计价、原子取消与退款。
- 【生产 API 未具备可用条件】生产数据库中的 `image-to-video` 工作流状态不是 `active`，`input_schema` 与 `output_schema` 为空，工具和工作流价格均为 `0`，且没有已验证的 poster/preview 视频。因此生产页面正确显示“工作流配置中 / 价格配置中 / 即将上线”，不会允许付费提交。

## 2. 现状审计与根因

### 2.1 数据来源

| 数据 | 当前来源 | 审计结果 |
|---|---|---|
| 工具信息 | Supabase `tools` | `image-to-video` 存在，状态为 `published`，价格字段为 0 |
| 工作流 | Supabase `workflows` | `workflow-hifun-image-to-video-v1` 存在，状态为 `published`，不是消费者前台要求的 `active` |
| 参数能力 | `workflows.input_schema` | 当前为空，无法证明时长、比例、镜头运动等参数受后端支持 |
| 输出能力 | `workflows.output_schema` | 当前为空 |
| 价格 | `tools` / `workflows` / `input_schema.pricing` | 当前均没有可用的非零生产配置 |
| 效果映射 | `apps/web/workflow-map.json` 与前端候选清单 | 候选工作流均标记为需要服务端配置，不能视为已验证 active workflow |
| 任务 | Supabase `generation_jobs` | 已有状态、时长、比例、分辨率、结果、错误、积分等字段，可复用 |
| 退款 | Supabase `credit_transactions` | 已复用现有退款记录，不创建重复积分系统 |

### 2.2 主要问题

1. 页面原来只有通用参数，无法区分后端是否真实支持。
2. 视频效果没有统一视觉选择器和真实可用性门控。
3. 前端显示价格与服务端估算逻辑没有统一扩展点。
4. 幂等键只在工作台生成，但没有贯通到桥接层和 Edge Function。
5. 多个相同请求可能同时触发任务处理。
6. 取消任务原来由浏览器直接更新状态，无法保证退款和竞态安全。
7. 任务状态只覆盖排队、运行、成功、失败，缺少视频生成阶段的用户文案。
8. 完成结果缺少视频元数据和视频专用重试操作。

## 3. 修改文件

### `apps/web/generation-workspace.js`

- 新增视频效果候选清单和视觉 `EffectPickerModal` 支持。
- 效果分类支持：通用、热门、最新、第一人称、姿势。
- 没有真实媒体时显示“预览准备中”，不显示黑卡、不伪造 poster。
- 只有与当前 `active` 工作流匹配的效果才可确认。
- 根据工作流 `input_schema` 条件化显示：
  - 视频时长
  - 输出比例
  - 镜头运动
  - 运动强度
  - 人脸稳定
  - 是否循环
  - 分辨率
  - 输出数量
- 生产 schema 为空时不显示伪造参数，并说明配置未发布。
- 根据统一 pricing 配置动态计算积分：
  - `duration_costs`
  - `resolution_multipliers`
  - `output_multipliers`
- 价格不存在或为 0 时显示“价格配置中”，不显示免费。
- 积分不足时显示购买入口。
- 新增并映射任务阶段：
  - 上传中
  - 等待队列
  - 模型准备
  - 生成中
  - 后处理
  - 上传结果
  - 完成
  - 失败
  - 超时
- 进度条仅使用服务端 `progress`；没有服务端进度时使用不含百分比的等待状态。
- 结果区新增视频元数据：时长、分辨率、文件大小、创建时间、使用效果。
- 结果操作支持：播放、下载、重新生成、使用新描述重试、保存、分享、复制参数。
- 取消任务改为调用服务端取消接口，不再由前端直接改写数据库状态。
- 最近任务查询补充视频元数据字段。
- 保留 sessionStorage 的最近任务 ID，刷新后从真实任务表恢复。
- 新增仅限 localhost 的 QA 能力配置，用于验证 2/5/10 秒和动态价格；不会进入生产数据。

### `apps/web/app.js`

- 将 `idempotencyKey` 从工作台贯通到真实生成桥接层。
- 贯通视频效果、镜头运动、运动强度、人脸稳定、循环、分辨率、输出数量。
- 工作流优先使用已选择且通过可用性检查的工作流 ID。
- 新增桥接层 `cancel`，调用服务端取消任务和退款逻辑。

### `supabase/functions/ai/index.ts`

- 接受并白名单化图片转视频消费者参数。
- 客户端价格只保存为审计 quote，不用于可信扣费。
- 服务端从工具、工作流和 `input_schema.pricing` 重新计算权威积分。
- 幂等键按“用户 ID + 幂等键”生成稳定任务 ID。
- 重复创建请求返回已有任务，不重复插入和扣费。
- 任务处理使用条件更新进行原子领取，同一任务只允许一个处理者执行。
- 取消任务先原子领取为 `cancelling`，再退款并更新为 `cancelled`，避免与生成处理竞态。
- 失败继续复用现有自动退款逻辑。

### `apps/web/styles.css`

- 新增视频参数响应式网格。
- 新增视频布尔选项布局。
- 新增视频效果预览媒体样式。
- 新增结果元数据布局。

## 4. 价格规则

前端与服务端均支持以下统一配置形状：

```json
{
  "pricing": {
    "duration_costs": {
      "2": 24,
      "5": 36,
      "10": 60
    },
    "resolution_multipliers": {
      "720p": 1,
      "1080p": 1.5
    },
    "output_multipliers": {
      "1": 1,
      "2": 2
    }
  }
}
```

服务端始终重新计算价格，不信任浏览器传入的 `priceQuote`。

## 5. 验证结果

### 5.1 自动检查

| 检查 | 结果 |
|---|---|
| `npm run typecheck` | 通过 |
| `npm run build` | 通过 |
| `git diff --check` | 通过 |
| `node --check apps/web/generation-workspace.js` | 通过 |
| `node --check apps/web/app.js` | 通过 |

构建仍输出项目既有警告：部分非 module 脚本无法参与 Vite bundling，以及既有 `home-assets` 路径在构建时未解析。本阶段没有扩大这些警告，也不属于图片转视频流程范围。

### 5.2 浏览器验证

测试地址使用本地统一工具页：

`/tool.html?tool=image-to-video`

验证结果：

| 场景 | 结果 |
|---|---|
| 生产配置降级 | 显示“工作流配置中”“价格配置中”，参数不伪造 |
| 生产效果弹层 | 5 个候选均显示“即将上线”，确认按钮不可用 |
| 视觉效果分类 | 通用、热门、最新、第一人称、姿势可筛选 |
| 2/5/10 秒 | localhost QA schema 下可选择 |
| 动态价格 | 2 秒 + 720p + 1 个输出 = 24；10 秒 + 1080p + 2 个输出 = 180 |
| 积分不足 | 余额 100、价格 180 时显示购买积分入口 |
| 上传中 | 显示“上传中”，不伪造百分比 |
| 等待队列 | 显示任务号、创建时间和服务端账单价格 |
| 模型准备 | 显示“模型准备”，无服务端进度时显示等待文案 |
| 生成中 | 显示“生成中” |
| 后处理 | 显示“后处理” |
| 上传结果 | 显示“上传结果” |
| 失败退款 | 已有退款记录时显示“已退回 24 积分” |
| 超时 | 显示超时说明、退款核对和人工处理提示 |
| 重复提交 | 前端提交锁 + 服务端稳定任务 ID + 原子领取 |
| 刷新恢复 | 通过 sessionStorage 最近任务 ID + `generation_jobs` 重新查询恢复 |

### 5.3 截图

- `docs/screenshots/phase7-image-to-video-desktop.jpg`
- `docs/screenshots/phase7-effect-picker-production.jpg`

## 6. 未完成项

### 6.1 生产工作流激活

当前生产工作流不是 `active`。需要运营或工作流负责人完成真实运行验证后，将消费者可用工作流标记为 `active`。在此之前前端不会允许创建任务。

### 6.2 生产参数 schema

需要在 `workflows.input_schema` 中发布真实支持的参数。未发布的参数不会显示。

### 6.3 生产价格配置

需要配置非零基础价格或 `input_schema.pricing`。在完成前页面显示“价格配置中”，不会显示 0 积分。

### 6.4 真实效果媒体

当前没有已验证的 poster 和 3—5 秒预览视频。所有候选使用非误导性“预览准备中”。有真实素材后再填入媒体 URL。

### 6.5 真实端到端生成与下载

由于没有 active workflow，本阶段没有执行真实付费生成、真实退款或真实下载。播放器、下载、保存、分享和复制参数已按真实 `result_url` 实现，但生产端到端验收必须等待工作流激活、Edge Function 部署和真实结果返回。

### 6.6 Edge Function 部署

`supabase/functions/ai/index.ts` 的服务端改动只完成在代码中，本任务没有自动部署生产函数。部署前需要在测试环境复核现有 secrets、工作流提供商和退款 RPC。

## 7. 下一任务依赖

1. 工作流负责人提供并验证至少一个 active 图片转视频工作流。
2. 为工作流发布真实 `input_schema`、`output_schema` 和 pricing。
3. 提供合法、已授权的 poster 与短预览视频。
4. 在测试环境部署 Edge Function 后执行：
   - 同一幂等键并发提交
   - 取消与生成并发
   - 积分扣减失败
   - 提供商失败退款
   - 超时转人工
   - 刷新恢复
   - 真实 MP4 下载
