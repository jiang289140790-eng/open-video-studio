# HOMEPAGE / TOOLS UI ACCEPTANCE

日期：2026-07-26
范围：消费者首页、图片工具目录、视频工具目录
状态：`BUILD_PASSED`

## 1. 修改前问题

1. 首页虽然已有上传入口，但缺少清晰的提示词输入，“上传—描述—选择类型—创建”的主路径不完整。
2. 首页内容仍包含多个展示模块，推荐工具、图片能力、视频能力和最近创作的层级不够清晰。
3. 图片工具目录使用“工作流”“模型”等内部术语，不适合普通用户。
4. 视频工具目录展示了未经真实 E2E 验收的模型、时长、积分和“立即生成”按钮。
5. 视频工具目录引用了外部站点图片，存在资源失效、授权和品牌混用风险。
6. 未接后端的卡片可以跳入生成页，容易形成假生成体验。
7. 图片、视频目录使用不同卡片结构和信息层级。
8. 工具目录没有统一表达 `featured`、`popular`、`preview`、`active`、`coming_soon` 和 `restricted` 状态。
9. 图片与视频两条 E2E 报告当前均为 `BLOCKED`，但旧目录仍出现“立即生成”表达。
10. `/zh/referral/` 路由别名缺失，首页推荐好友链接检查失败。

## 2. 修改后结构

### 首页

首页已按以下顺序收敛：

1. 顶部导航
2. Hero 主生成入口
3. 热门效果
4. 推荐工具
5. 图片创作能力
6. 视频创作能力
7. 最近创作
8. 积分/套餐入口
9. 页脚

Hero 只保留一条主要路径：

`上传参考图片 → 输入创意描述 → 选择创建图片/视频 → 选择已验证能力 → 开始创建`

当前没有达到 `E2E_VERIFIED` 的能力，因此：

- 生成按钮保持禁用；
- 页面明确显示“真实生成能力验收中”；
- 不进入假生成页面；
- 不调用或修改现有真实生成 API。

### 热门效果与推荐工具

- 不再使用随机网络图片。
- 使用统一的品牌渐变占位视觉，不拉伸现有图片。
- 卡片明确显示“精选”“热门”“预览”展示级别。
- 卡片明确显示“可创建”“即将上线”“受限内容”可用状态。
- 只有 `availability: active` 的卡片才会生成可点击链接。
- 当前没有卡片被标记为 `active`。

### 图片工具目录

面向用户展示：

- AI 图片生成
- 图片编辑
- 多图合成
- AI 换脸
- 更换服装
- 姿势生成
- 高清修复

不再显示模型名称或内部工作流名称。

### 视频工具目录

面向用户展示：

- 图片转视频
- 通用动态
- 视频特效
- 热门模板
- 最新模板

移除了旧页面中未经真实验证的模型名称、固定价格、处理时间和外部媒体图片。

### 最近创作

- 未登录时显示登录入口。
- 已登录时通过当前 Supabase 客户端和现有 RLS 查询当前用户最近 4 条 `generation_jobs`。
- 没有记录或读取失败时显示明确空状态。
- 未创建新的数据表或后端接口。

## 3. 状态规则

| 状态 | 用途 | 当前行为 |
|---|---|---|
| `featured` | 首页重点展示 | 显示“精选” |
| `popular` | 推荐与热门排序 | 显示“热门” |
| `preview` | 可预览的展示项 | 显示“预览” |
| `active` | 已通过真实 E2E | 可点击并创建 |
| `coming_soon` | 尚未完成真实 E2E | 显示“即将上线”，不可点击 |
| `restricted` | 受年龄、授权或内容政策限制 | 显示“受限内容”，不可点击 |

图片与视频 E2E 报告目前均为 `BLOCKED`，所以本轮没有把任何工作流标记为 `active`。

## 4. 修改文件

- `apps/web/app.html`
- `apps/web/homepage.js`
- `apps/web/image-tools.html`
- `apps/web/video-tools.html`
- `apps/web/tool-directory.js`
- `apps/web/styles.css`
- `apps/web/consumer-shell.js`
- `apps/web/public/zh/referral/index.html`

本轮没有修改：

- Supabase Edge Function
- Supabase 数据库和 RLS
- 工作流映射
- ComfyUI / Zealman
- 积分扣减
- 支付
- 生成 API

## 5. 页面截图

本地实际页面截图：

- `C:\Users\admin\Documents\工作流模型\_repo\artifacts\homepage-1920.png`
- `C:\Users\admin\Documents\工作流模型\_repo\artifacts\homepage-content-1920.png`
- `C:\Users\admin\Documents\工作流模型\_repo\artifacts\image-tools-1920.png`
- `C:\Users\admin\Documents\工作流模型\_repo\artifacts\video-tools-1920.png`

截图检查确认：

- Header 只有一组；
- Sidebar 只有一组；
- 首页 Hero 在 1920×863 可视区域中完整显示主要输入与 CTA；
- 工具目录首屏可看到筛选、搜索和首排卡片；
- 缺少媒体时使用统一占位视觉，没有破图或随机网络图片；
- 桌面端没有页面级横向溢出。

## 6. 响应式与手机端检查

| 宽度 | 检查结果 |
|---|---|
| 1920px | 浏览器实际渲染和截图检查通过 |
| 1440px | 桌面网格、最大内容宽度和间距规则已覆盖 |
| 1366px | 桌面网格保持三列，内容容器不会横向溢出 |
| 768px | 命中 `max-width: 820px`：目录两列、工具栏纵向排列、消费者侧栏变抽屉 |
| 390px | 命中 `max-width: 520px`：所有卡片单列、筛选横向滚动、Hero 上下排列 |

自动测试中的 `mobile visual safety` 子测试通过。

限制：当前浏览器检查环境固定为 1920px，因此本轮没有生成独立的 1440、1366、768 和 390 浏览器截图；这些尺寸完成了 DOM/CSS 断点审计，但仍建议上线前使用设备模拟器补充逐尺寸像素截图。

## 7. 链接检查

检查页面：

- `apps/web/app.html`
- `apps/web/image-tools.html`
- `apps/web/video-tools.html`

结果：

- 断链：0
- 新增 `/zh/referral/` 路由别名，修复推荐好友入口。
- 未验收卡片不包含生成页链接。

## 8. 构建与测试

| 检查 | 结果 |
|---|---|
| `npm run typecheck` | 通过 |
| `npm run lint` | 未执行成功：项目没有 `lint` script |
| `npm run build` | 通过 |
| `npm test` | 未全部通过：75 项中 60 通过、15 失败 |
| 页面截图检查 | 1920px 首页、图片目录、视频目录通过 |
| 链接检查 | 通过，0 个断链 |
| `git diff --check` | 通过 |

完整测试失败不是运行中断：

- 多项为项目原有静态页面、支付配置和 SEO 断言；
- 部分旧断言仍要求旧工具目录包含“locked”卡片、模型参数和预设按钮，与本任务“未 E2E 不得进入假生成”的规则冲突；
- 本轮没有通过恢复误导性按钮来迎合旧断言。

构建仍报告项目已有警告：

- 若干非模块脚本不能由 Vite 合并；
- 旧 `home-assets` CSS 引用在构建时无法解析。

这些警告没有阻止本轮构建，但需要后续独立处理。

## 9. 仍未完成

1. 图片和视频真实 E2E 均未通过，因此首页和目录没有 `active` 创建卡片。
2. 没有新增真实效果媒体；当前缺少媒体的卡片使用统一非误导性占位视觉。
3. 独立的 1440、1366、768 和 390 浏览器截图尚未生成。
4. 项目缺少 `npm run lint` 配置。
5. 旧静态测试需要按新的“真实 E2E 才可创建”规则更新，但本任务没有修改测试标准。
6. 其他工具详情页、辣味效果页和生成工作台不在本任务范围内，未重构。

## 10. 结论

首页与工具目录已经从“内部功能卡片集合”收敛为面向普通用户的创作入口，并且没有改变现有真实生成链路。

当前状态为 `BUILD_PASSED`，不是 `E2E_VERIFIED` 或生产验收通过。只有图片或视频链路完成真实 E2E 后，才能把对应卡片切换为 `active`。
