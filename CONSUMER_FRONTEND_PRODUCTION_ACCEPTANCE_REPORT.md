# Phase 12 — Luravyn 消费者前台生产验收报告

> 验收日期：2026-07-26
> 本地项目：`C:\Users\admin\Documents\工作流模型\_repo`
> 生产网站：`https://jiang289140790-eng.github.io/open-video-studio/app.html`
> Supabase 项目：`open-video-studio`（生产项目，未创建第二套项目）
> 结论：**未通过生产验收，不可宣称生产就绪**

## 1. 执行范围与证据边界

本次执行了：

- 本地代码、路由、任务、积分、支付、作品、分享、推荐与埋点的静态审计。
- 生产 GitHub Pages 页面只读浏览器检查。
- 生产 Supabase 表结构、行数、RLS/安全与性能 Advisor 的只读检查。
- TypeScript 检查、正式构建、专项回归测试、完整测试套件和 Git diff 检查。
- 补齐现有本地分析系统的标准事件名及敏感字段过滤。

本次没有执行：

- 没有创建或删除生产用户。
- 没有给生产账户增加、扣除或退还积分。
- 没有提交真实 AI 任务。
- 没有执行真实或测试付款。
- 没有应用生产数据库迁移。
- 没有推送 GitHub 或部署 GitHub Pages。

因此，报告会区分“代码中已实现”“线上已部署”“线上已用真实数据验证”三个层级。缺少真实证据的路径一律不判定为通过。

## 2. 总体完成度

| 范围 | 完成度 | 判定 |
|---|---:|---|
| 本地消费者前端代码 | 68% | 主要页面和状态组件存在，但完整回归仍有失败 |
| 生产 GitHub Pages | 25% | 当前部署为旧版本，未包含新的首页和消费者工作台 |
| 生产 Supabase 数据结构 | 42% | 基础表存在，但 Phase 9/10/分享相关迁移未应用 |
| 生产核心闭环 | 15% | 四条核心路径均未获得真实端到端通过证据 |
| 安全上线条件 | 20% | 存在公开 Schema 敏感表未启用 RLS 等 P0 |
| **生产验收综合完成度** | **28%** | **不具备上线条件** |

## 3. 生产现状

### 3.1 GitHub Pages

生产首页返回成功，但浏览器读取到的是旧构建：

- 页面标题：`AI 工具 | Luravyn`
- 当前脚本哈希包含：`assets/app-D2TrmqO-.js`
- 页面仍显示旧动漫 Banner、旧卡片和旧浮动按钮。
- 未检测到新的首页上传 Hero。
- 页面 DOM 没有新的 Visual System 标记。
- 1280 像素视口下未发现横向溢出。

结论：本地 Phase 1—11 的多数改造没有部署到生产 GitHub Pages，生产页面不能作为当前代码的功能证明。

截图：

- `artifacts/phase12/production-home.png`

### 3.2 生产 Supabase 数据

只读统计结果：

| 表 | 行数 |
|---|---:|
| profiles | 0 |
| credits | 0 |
| credit_transactions | 0 |
| generation_jobs | 0 |
| media_assets | 0 |
| user_creations | 0 |
| orders | 0 |
| share_links | 0 |
| referral_codes | 0 |
| referral_events | 0 |
| workflows | 3 |

三个工作流均为 `published`，但积分成本均为 `0.0000`：

- AI 换脸：`workflow-hifun-face-swap-v1`
- 图片编辑器：`workflow-hifun-image-editor-v1`
- 图片转视频：`workflow-hifun-image-to-video-v1`

这与前端非零价格配置不一致。没有用户、积分、任务、订单或作品记录，无法证明任何真实消费者闭环已经成功运行。

### 3.3 数据库版本不一致

本地存在但生产未应用：

- `202607260001_consumer_creation_shares.sql`
- `202607260002_consumer_credit_checkout.sql`
- `202607260003_free_credits_referrals.sql`

生产缺少或不完整：

- `reward_grants` / `reward_claims`
- `daily_checkins`
- `referral_attributions`
- `creation_shares`
- `orders.package_id`
- `orders.offer_code`
- `orders.idempotency_key`
- `orders.order_metadata`
- `share_links.expires_at`
- 订单幂等唯一索引
- 积分订单发放唯一索引
- 推荐奖励唯一约束

当前已部署 AI Edge Function 使用了这些新结构。代码版本和数据库版本不一致会导致支付、分享、签到、推荐等请求在运行时失败。

## 4. 核心路径验收

### 路径 A：免费首次生成

流程：

首页 → 选择效果 → 上传图片 → 登录 → 领取/使用免费积分 → 创建任务 → 完成 → 查看作品 → 下载

| 节点 | 代码状态 | 生产状态 |
|---|---|---|
| 首页选择效果 | 本地已实现 | 生产仍为旧首页 |
| 上传图片 | 本地有上传状态与 Supabase Storage 逻辑 | 未用生产用户验证 |
| OAuth/Session | 代码存在 | 生产无用户记录，未验证 |
| 免费积分 | Edge Function 与本地迁移存在 | 生产奖励表缺失 |
| 创建任务 | Edge Function 存在 | 无任务记录，工作流价格为 0 |
| 失败退款 | 服务端代码存在 | 无真实交易证据 |
| 保存作品 | 服务端代码存在 | 无作品或媒体资产 |
| 下载 | 前端操作存在 | 无真实生成结果可下载 |

结果：**失败 / 阻塞**。

阻塞原因：

1. 新首页没有部署。
2. 免费积分数据库迁移未应用。
3. 没有真实用户、余额和任务记录。
4. 工作流成本与前端价格不一致。

### 路径 B：积分不足与支付

流程：

工具页 → 配置任务 → 积分不足 → 价格页 → 选择套餐 → 支付测试流程 → 积分回写 → 返回工具页 → 生成

| 节点 | 代码状态 | 生产状态 |
|---|---|---|
| 积分不足跳转 | 本地已实现 | 未生产验证 |
| 套餐统一配置 | 本地已实现 | 生产页面仍为旧构建 |
| 支付方式显隐 | 本地只显示已配置方式 | 未生产验证 |
| 重复点击保护 | `checkoutInFlight` 存在 | 未真实验证 |
| Webhook 回写 | Billing Function 有签名校验与服务端发放逻辑 | 无订单记录 |
| 订单幂等 | 本地迁移有设计 | 生产缺少字段/唯一索引 |
| 返回后确认 | 前端轮询服务端 `fulfilled` 状态 | 生产结构不匹配 |

结果：**失败 / 阻塞**。

关键风险：

- 生产 `orders` 表缺少当前 Edge Function 需要的字段。
- 生产没有订单幂等唯一约束，不能证明重复点击不会重复发放。
- 旧的演示结账代码仍可产生本地“已到账”状态；这不能作为真实支付成功，也不得用于生产统计。
- 本次没有执行真实付款或制造支付成功记录。

### 路径 C：图片转视频

流程：

作品中心 → 选择图片 → 转成视频 → 选择效果 → 创建任务 → 查看视频 → 下载

| 节点 | 代码状态 | 生产状态 |
|---|---|---|
| 消费者作品中心 | 本地已有简化版 | 生产未部署 |
| 图片“转成视频” | 本地操作存在 | 无作品可验证 |
| 视频视觉效果选择 | 本地组件存在 | 无生产实测 |
| 动态积分 | 本地统一价格逻辑存在 | 生产工作流成本为 0 |
| 任务恢复 | 本地有任务 ID/刷新恢复逻辑 | 无真实任务 |
| 下载 | 本地有下载操作 | 无真实视频 |

结果：**失败 / 阻塞**。

### 路径 D：推荐

流程：

免费积分 → 复制链接 → 新用户注册 → 有效归因 → 奖励发放 → 进度更新

| 节点 | 代码状态 | 生产状态 |
|---|---|---|
| 复制推荐链接 | 本地已实现并补充埋点 | 无用户和推荐码 |
| 注册归因 | Edge Function 代码存在 | 新版归因表未部署 |
| 防自荐 | 服务端代码存在 | 无真实验证 |
| 首次生成条件 | 服务端代码存在 | 无成功任务 |
| 奖励幂等 | 本地迁移有唯一键和原子函数 | 生产未应用 |
| 进度更新 | 本地 UI 存在 | 无推荐事件 |

结果：**失败 / 阻塞**。

## 5. 状态覆盖

| 状态 | 代码覆盖 | 生产验证 | 结论 |
|---|---|---|---|
| 未登录 | 有 | 页面可见 | 部分通过 |
| 登录中 | 有 | 未验证 OAuth 回调 | 未通过 |
| 登录失败 | 有错误提示 | 未验证 | 未通过 |
| 积分加载失败 | 有降级提示 | 未验证 | 未通过 |
| 上传失败 | 有错误状态 | 未验证 Storage 失败 | 未通过 |
| 工作流不可用 | 有禁止提交逻辑 | 数据状态不一致 | 未通过 |
| 排队 | 有 queued 状态 | 无任务记录 | 未通过 |
| 生成 | 有 processing 阶段 | 无真实任务 | 未通过 |
| 成功 | 有 completed UI | 无真实结果 | 未通过 |
| 失败 | 有 failed UI | 无真实失败任务 | 未通过 |
| 退款 | 服务端有退款调用 | 无交易证据 | 未通过 |
| 支付取消 | 前端有取消返回处理 | 未验证 | 未通过 |
| 支付成功 | 仅信任服务端 fulfilled | 无订单证据 | 未通过 |
| 网络断开 | 有部分 catch/恢复 | 未进行离线浏览器测试 | 未通过 |
| 页面刷新 | 有任务恢复代码 | 无真实任务 | 未通过 |
| 重复点击 | 任务幂等键和支付锁代码存在 | 生产唯一约束不完整 | 未通过 |

## 6. 数据一致性审计

### 已有保护

- 生成任务先建立服务端记录，再调用积分扣减和工作流。
- 任务处理失败会调用退款逻辑。
- 取消任务先声明状态，再根据条件退款。
- Billing Webhook 验证签名后再更新订单和积分。
- 支付返回页只在数据库订单为 `fulfilled` 时显示到账。
- 任务创建支持幂等键参数。

### 无法通过或存在风险

- 生产没有任何真实任务、积分交易、订单、作品或分享记录，无法核对账实一致。
- 生产订单表缺少幂等字段和唯一索引。
- 生产推荐奖励与免费积分表未部署，无法保证不重复奖励。
- `generation_jobs` 没有明确持久化的 `idempotency_key` 字段；目前依赖确定性任务 ID，需在并发测试中证明。
- 多张表存在重复 permissive RLS policy，规则可维护性和查询性能较差。
- 工作流数据库成本为 0，而前端展示非零价格，可能造成报价、扣费和统计不一致。

## 7. 埋点验收

项目没有 PostHog；现有分析实现为用户同意后写入浏览器本地存储，没有远程上报。

本次补齐/规范的事件：

- `homepage_view`
- `effect_view`
- `effect_select`
- `upload_start`
- `upload_success`
- `generation_submit`
- `generation_queued`
- `generation_completed`
- `generation_failed`
- `pricing_view`
- `checkout_start`
- `checkout_success`（仅服务端订单确认 `fulfilled` 后）
- `creation_download`
- `creation_share`
- `referral_copy`
- `daily_reward_claim`

额外处理：

- 演示积分完成事件改记为 `checkout_simulated`，不冒充真实 `checkout_success`。
- 支付取消记录为 `checkout_cancelled`。
- 分析属性会拒绝包含以下敏感字段名的数据：prompt、token、secret、key、authorization、cookie、image、file、blob、card、payment、url。
- 只允许空值、字符串、数字和布尔值，字符串最长 160 字符。

限制：

- 当前只有浏览器本地事件，没有服务端可信漏斗和跨设备统计。
- 未执行真实事件上报验证。

## 8. 性能检查

正式构建主要产物：

| 产物 | 原始大小 | gzip |
|---|---:|---:|
| 主应用大包 | 430.20 kB | 117.28 kB |
| 主应用辅助包 | 190.45 kB | 60.08 kB |
| generation-workspace | 80.11 kB | 22.68 kB |
| 全局 CSS | 198.60 kB | 38.93 kB |

发现：

- 构建成功，但多个普通 `<script>` 不能被 Vite 打包，影响缓存依赖与构建可追踪性。
- 12 个首页资源在构建期无法解析，保留为运行时路径；这是生产破图风险。
- 主 JS 包接近 430 kB，消费者代码和运营代码仍有较强耦合，需拆包。
- 媒体卡已有 lazy loading、poster 和移动端避免批量自动播放的实现。
- 生产首页 1280 像素宽度下没有横向溢出。
- 由于生产版本过旧，本次无法用生产版本完成 390×844、430×932、1440×900、1920×1080 的新界面验收。

## 9. 安全检查

### P0 安全问题

Supabase Security Advisor 报告生产 `public` Schema 中多张表没有启用 RLS，其中包含：

- `users`
- `sessions`
- `oauth_clients`
- `password_reset_tokens`
- `accounts`
- 以及多张运营/自动化历史表

Advisor 还识别出公开敏感列：

- `users.password`
- `oauth_clients.secret`
- `password_reset_tokens.token`

其他高风险项：

- `current_profile_role()` 为 SECURITY DEFINER 且可被 anon/authenticated 调用。
- `debit_credits(integer)` 为 SECURITY DEFINER 且可被 authenticated 调用。
- `content_workflow_runs` 启用了 RLS 但没有 policy。
- 多个函数 search_path 可变。
- 多张消费者表有重复 permissive policies。

### 其他检查

- 仓库中没有发现真实 `.env.local`，只有示例配置文件。
- 未读取或输出任何现有密钥完整值。
- 浏览器代码没有 service_role 密钥。
- Storage 上传按用户路径写入并尝试签名 URL，但读取逻辑仍会先计算 public URL；必须确认生产 Bucket 为私有并只使用签名读取。
- 分享链接的过期/撤销新结构尚未部署。
- 前端对展示文字普遍使用转义，但动态 HTML 和 URL 仍需继续做 XSS/协议白名单测试。
- 管理后台业务代码仍与消费者大包同仓，生产路由和授权必须持续隔离。

## 10. 页面完成度

| 页面 | 本地完成度 | 生产完成度 | 主要阻塞 |
|---|---:|---:|---|
| 首页 | 78% | 20% | 新首页未部署 |
| 辣味效果 | 70% | 25% | 生产旧卡片/工作流映射未证实 |
| 图片编辑器 | 72% | 30% | 无真实任务证据 |
| AI 换脸 | 68% | 30% | 无人脸检测/工作流生产验证 |
| 性感礼服 | 65% | 25% | 预设与真实工作流绑定未证实 |
| 性爱姿势 | 65% | 25% | 预设与真实工作流绑定未证实 |
| 图片转视频 | 72% | 30% | 工作流价格为 0、无结果 |
| 我的作品 | 68% | 20% | 生产旧版且无作品数据 |
| 购买积分 | 60% | 15% | 生产订单结构落后 |
| 免费积分 | 62% | 10% | 奖励/归因迁移未部署 |

## 11. 问题分级

### P0 — 上线阻塞

1. **生产 GitHub Pages 是旧构建。**
2. **生产数据库未应用最近三项消费者迁移，代码与 Schema 不兼容。**
3. **公开 Schema 存在敏感表未启用 RLS，且暴露密码、OAuth Secret、重置 Token 列。**
4. **四条核心路径均没有真实端到端通过证据。**
5. **生产订单幂等字段/唯一约束缺失，不能安全验收支付。**
6. **奖励与推荐原子发放结构未部署。**
7. **工作流成本为 0，与前端报价不一致。**

### P1 — 高优先级

1. 清理或隔离旧演示结账，生产用户不得获得本地虚拟“已到账”状态。
2. 对私有 Storage 只发签名 URL，确认 Bucket 和对象 policy。
3. 合并重复 RLS policy，并补齐未索引外键。
4. 将消费者与运营后台代码拆包，减少 430 kB 主包和攻击面。
5. 解决构建期无法解析的首页资源路径。
6. 建立服务端可信 Analytics Sink；当前事件只保存在本地。
7. 在 staging 使用测试账户完成 OAuth、上传、生成、退款、Webhook、分享、推荐并发测试。

### P2 — 改进项

1. 清理 `defaultState` 中的演示用户、订单、作品和分享数据，确保生产永不显示为真实数据。
2. 补齐 canonical/hreflang 与旧静态测试要求。
3. 将普通脚本转为模块或纳入 Vite 入口。
4. 增加网络断开、慢网、移动端和浏览器兼容自动化测试。

## 12. 构建与测试结果

| 检查 | 结果 |
|---|---|
| `npm run typecheck` | 通过 |
| `npm run build` | 通过，存在脚本与 12 个首页资源路径警告 |
| Phase 9/10/11 专项测试 | 13/13 通过 |
| 完整 `npm test` | 63/75 通过，12 项失败 |
| `git diff --check` | 通过 |

完整测试失败集中在：

- 旧静态前端契约与 Phase 1—11 新结构不一致。
- referral 页面共享样式契约。
- 旧 CTA、筛选、工作台和作品中心选择器。
- 旧支付配置测试要求。
- pricing canonical 元数据。

这些失败不是本次埋点修改造成的，但它们说明当前分支不能视为回归测试全绿。

## 13. 本次修改文件

- `apps/web/app.js`
  - 统一漏斗事件名。
  - 增加首页、价格、效果、上传、排队、下载、推荐和签到事件。
  - 真实支付成功仅在服务端订单确认后记录。
  - 演示积分使用 `checkout_simulated`。
  - 增加敏感分析属性过滤。
- `CONSUMER_FRONTEND_PRODUCTION_ACCEPTANCE_REPORT.md`
  - 本报告。

## 14. 上线建议

当前建议：**禁止直接全量上线**。

推荐顺序：

1. 立即在 Supabase 中隔离或撤出不属于消费者公开 API 的旧表，并为所有保留的公开表启用正确 RLS。
2. 撤销 anon/authenticated 对敏感 SECURITY DEFINER 函数的权限。
3. 在数据库分支或 staging 先应用三项待部署迁移。
4. 校验 Edge Functions 与 staging Schema 一致。
5. 将工作流价格同步到统一定价配置，禁止生产 `0` 成本误用。
6. 部署当前 GitHub Pages 构建，并确认脚本哈希发生变化。
7. 使用专用测试用户和支付 Sandbox 完成四条路径。
8. 对任务、积分交易、订单、作品、资产、分享与推荐记录逐条对账。
9. 只有 P0 清零、完整测试通过、四条核心路径真实通过后，才可进入小流量灰度。

## 15. 回滚方式

### 前端

- 为上线构建记录 Git commit 和 GitHub Pages artifact。
- 发生问题时，将 Pages 切回上一个已验证 artifact/commit。
- 不在回滚时覆盖用户上传的 Storage 数据。

### Edge Functions

- 上线前保存当前版本号和环境变量清单（只记录变量名，不记录值）。
- 新版本异常时重新部署上一个已验证函数版本。

### 数据库

- 应用迁移前创建 Supabase 数据库备份或确认 PITR 可用。
- 优先使用向前修复迁移，不执行破坏性 down migration。
- 如果迁移导致不可恢复错误，使用备份/PITR 恢复到明确时间点。
- 恢复后逐项核对订单、积分交易、任务与作品，禁止只恢复余额不恢复账本。

## 16. 最终判定

**生产验收：未通过。**

当前不能宣称：

- 登录闭环已验证；
- 免费首次生成可用；
- 支付后积分一定正确回写；
- 失败任务一定退款；
- 图片可转视频并下载；
- 推荐奖励可以防重复发放；
- 生产数据与前端状态一致。

只有完成 P0 修复、部署新前端、同步生产 Schema，并用隔离测试账户跑通四条核心路径后，才能重新申请生产验收。
