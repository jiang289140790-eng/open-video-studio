# AI 图片生成页 UI 优化报告

状态：`DEPLOYED` / `ONLINE_VERIFIED`

线上页面：
https://jiang289140790-eng.github.io/open-video-studio/generate.html

UI 部署提交：
`033aae85288cc09260a89f06a0fd6bbd52304b8d`

GitHub Pages 发布任务：
https://github.com/jiang289140790-eng/open-video-studio/actions/runs/30231075945

## 1. 修改范围

本次只修改了图片生成页的页面结构、视觉样式和前端交互层级：

- `apps/web/generate.html`
- `apps/web/generation-workspace.js`
- `apps/web/styles.css`

未修改：

- A01 工作流及节点映射
- Supabase 表结构
- 积分扣费与退款逻辑
- Edge Function 接口
- Zealman / ComfyUI 调用接口
- 其他工具页面

## 2. 删除和收敛的重复内容

- 删除“当前为纯文生图”整块说明卡片。
- 页面标题区收敛为“AI 图片生成”、一句副标题、`Qwen Image` 与“文生图”标签。
- 删除输入框下方常驻的完整提示词列表。
- 删除“成人主题示例”标题及公共页面常驻敏感示例。
- 删除独立的“积分确认”大卡片，改为按钮上方一行积分摘要。
- 生成成功状态只保留一组操作入口。
- 最近任务由完整任务列表收敛为最近 3 项。
- 将大型全站页脚替换为图片生成工具页简化页脚。
- 修复工具页页脚继承全站宽度后产生的横向溢出。

## 3. 新页面结构

### 标题区

- 返回工具首页
- `Qwen Image` / “文生图”标签
- AI 图片生成
- 简短说明
- 工作流实时可用状态

### 左侧参数区

1. 图片描述
2. 优化描述 / 灵感示例 / 清空
3. 图片比例
4. 生成数量（A01 当前固定为 1 张）
5. 高级设置（Seed、隐私）
6. 预计积分 / 当前余额或访客免费体验说明
7. 开始生成按钮

### 灵感示例

由常驻文本改为弹窗，包含：

- 写真人像
- 时尚摄影
- 电影氛围
- 动漫角色
- 产品摄影

选择模板后会填入输入框，用户仍可继续修改。

### 右侧结果区

保留并统一以下真实任务状态：

- `empty`
- `queued / running`
- `succeeded`
- `failed`

成功状态主操作为“下载”，再次生成和转视频为次操作，保存、复制参数、分享及“我的创作”进入更多菜单。

### 最近生成

- 仅显示最近 3 个结果。
- 加载失败使用统一占位，不显示浏览器破图图标。
- 完整历史进入“我的创作”。

## 4. 保留并回归的功能

- A01 提交入口和原服务端代理调用路径。
- 任务状态轮询。
- 页面刷新后的任务恢复逻辑。
- 成功图片展示。
- 下载。
- 再次生成。
- 转视频。
- 保存到我的创作。
- 登录状态与积分摘要。
- 失败退款提示。
- 真实工作流不可用时继续禁用提交，不显示假成功。

未在本次任务中触发真实付费生成，避免产生额外生成费用；通过代码差异、构建和页面交互确认生成接口及服务端代码没有被修改。

## 5. 页面与交互验证

已检查：

- 1920px
- 1440px
- 1366px
- 768px
- 390px

结果：

- 各断点 `scrollWidth === clientWidth`，没有横向滚动。
- 390px 下为单列布局，生成按钮宽度为 330px，未溢出视口。
- 灵感弹窗显示 5 个分类模板。
- 选择模板后提示词正确回填，弹窗关闭，生成按钮状态随输入更新。
- 未填写提示词时“优化描述”和生成按钮保持禁用。
- “当前为纯文生图”“成人主题示例”均未出现在最终页面。
- 简化页脚与主内容之间无额外大块空白。
- 线上页面存在真实提交按钮、灵感入口和简化页脚。

## 6. 线上截图

桌面端（1440 × 900）：

`reports/screenshots/generate-page-desktop-online.png`

移动端（390 × 844）：

`reports/screenshots/generate-page-mobile-online.png`

截图均来自已部署的 GitHub Pages 页面，不是设计稿或合成图。

## 7. 工程验证

| 检查 | 结果 |
|---|---|
| `npm run typecheck` | 通过 |
| `npm run build` | 通过 |
| `git diff --check` | 通过 |
| 响应式 DOM 检查 | 通过 |
| 线上页面检查 | 通过 |
| `npm run lint` | `BLOCKED`：项目没有定义 `lint` script |
| `npm test` | 59/75 通过，16 个项目级既有断言失败 |

`npm test` 的失败集中在旧的静态页面、支付配置、SEO 与历史目标站断言，例如 referral 样式、旧 CTA、支付配置及 canonical 元数据；本次生成页的构建、移动端安全测试和新增交互检查均通过。本任务没有扩大范围去修改这些其他页面。

## 8. 仍未解决的问题

1. 项目缺少 `npm run lint` 命令，因此无法给出 lint 通过状态。
2. 项目级测试仍有 16 个与本任务范围无关的既有失败。
3. 线上工作流可用状态依赖现有服务端配置与异步健康检查；本次未修改该接口。若健康检查返回不可用，页面会按既有保护逻辑禁用提交并显示“即将上线”，不会进入假生成。
4. 本次只验证生成页 UI 与现有交互路径，没有执行新的真实 A01 付费生成。

## 9. 验收结论

- 页面 UI、信息架构和响应式布局：`ONLINE_VERIFIED`
- GitHub Pages 部署：`DEPLOYED`
- A01 接口未被本次修改：已确认
- 全项目测试：`BLOCKED`（16 个范围外既有失败）

