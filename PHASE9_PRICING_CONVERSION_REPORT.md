# Phase 9 — Pricing and Conversion UX Report

## 1. 范围与结论

本阶段完成消费者积分价格页、统一套餐配置、支付入口状态、优惠生命周期和服务端权威结账逻辑。没有执行真实付款，没有部署 Edge Function，没有运行生产数据库迁移，也没有修改生成 API、工作流或积分扣减逻辑。

关键结果：

- 套餐不再分散硬编码，前端统一读取 `consumer_credit_pricing`，远程未配置时使用同一个审计后的默认配置。
- 页面不再展示虚假原价、虚假折扣、虚假用户量、评分、满意度或测试评价。
- 预计生成数量根据当前已发布工具的非零积分价格动态计算。
- 未配置的支付方式不会显示给普通用户；没有可用支付方式时购买按钮禁用。
- 前端不再以价格、积分或成功返回 URL 作为到账依据。
- 相同购买尝试使用幂等键；服务端按套餐 ID 计算价格和到账积分。
- Stripe 付款完成后只由验签 Webhook 发放积分。
- 优惠统一为“额外 60%”：1000 → 1600，优惠码为 `WELCOME60`。
- 优惠倒计时持久化到当前设备，刷新不重置，45 秒后或退出意图触发，关闭后 24 小时限频。

## 2. 现状审计与根因

### 2.1 套餐来源

- 生产 `plans` 表现有 Free / Pro / Enterprise，属于会员或订阅方案，不是一次性积分包。
- 生产 `site_settings` 未发现 `consumer_credit_pricing`。
- 因此没有把订阅方案错误映射成一次性积分包；使用任务指定的四档套餐作为统一回退配置：
  - 200 / $9.99
  - 1000 / $29.99
  - 5000 / $99.99
  - 20000 / $299.99

### 2.2 工具价格

- 消费者前端当前已发布工具配置为：
  - 图片：8–40 积分
  - 视频：24–32 积分
- 生产 `tools` 表抽查到部分 `credit_cost=0`，不能用于估算。
- 新逻辑只使用已发布且大于 0 的工具价格；如果没有有效价格，显示“价格待配置”，不计算误导数量。

### 2.3 原购买页问题

- 套餐、价格和用途在 HTML 内重复硬编码。
- “50% / 83% / 90% OFF”没有真实计算依据。
- 原价、平均图片价格、平均视频价格为硬编码。
- 页面包含无法验证的 50K、4.9/5、99% 和用户评价。
- Stripe / PayPal 未配置仍展示。
- 真实结账失败后自动调用演示购买并发放积分。
- 本地回退会直接修改余额并创建已完成订单。
- 服务端信任前端传入的价格和积分。
- 付款返回 URL 没有严格区分“支付页面返回”和“Webhook 已确认到账”。

### 2.4 原优惠问题

- 文案为 2x，但实际为 1000 → 1600。
- 优惠码、到账积分和标题不一致。
- 进入页面约 450ms 就弹出。
- 倒计时是静态文本。
- 刷新后生命周期不可靠，只使用 sessionStorage 记录关闭状态。

## 3. 修改文件

### 新增

- `apps/web/pricing-config.js`
  - 统一套餐、优惠、正规化、单位价格、节省比例和生成数量估算。
- `tests/pricing-config.test.js`
  - 验证四档套餐、60% 加赠、单位价节省和零价格保护。
- `supabase/migrations/202607260002_consumer_credit_checkout.sql`
  - 为订单增加套餐、优惠、幂等和服务端元数据字段。
  - 为订单幂等键和积分发放增加唯一保护。

### 修改

- `apps/web/pricing.html`
  - 重建消费者购买页。
  - 删除乱码、测试卡通插图、虚假折扣、虚假评价和虚假指标。
  - 增加真实能力、统一套餐容器、动态用途和可验证承诺。
- `apps/web/app.js`
  - 动态加载统一套餐和工具价格。
  - 从服务端加载支付方式状态。
  - 隐藏未配置支付方式。
  - 推荐套餐、单位价和节省比例动态渲染。
  - 设备持久优惠、延迟触发、退出意图、限频、关闭和过期状态。
  - 创建结账时只提交 `packageId`、`offerCode` 和 `idempotencyKey`。
  - 取消返回不发积分；成功返回轮询服务端订单状态。
- `apps/web/styles.css`
  - 新增桌面与移动端购买页布局。
  - 推荐套餐、真实能力、用途、状态和结账弹层样式。
- `supabase/functions/ai/index.ts`
  - 支付方式状态可供价格页安全读取。
  - 服务端解析统一套餐，拒绝前端价格与积分。
  - 服务端验证优惠码并生成一致 metadata。
  - 相同幂等键拒绝重复建单。
  - 只有 Stripe 功能开关、密钥和 Webhook 都就绪时才公开为可用。
  - PayPal 在验签捕获 Webhook 未完成前保持隐藏。
- `supabase/functions/billing/index.ts`
  - Stripe `checkout.session.completed` 对一次性积分订单走独立处理。
  - 只在 `payment_status=paid` 且签名验证通过后发放积分。
  - 重复 Webhook 不重复发放积分。

## 4. 套餐计算结果

以 200 积分入门包单位价为基准：

| 套餐 | 价格 | 每 100 积分 | 单位价节省 |
|---|---:|---:|---:|
| 200 | $9.99 | $5.00 | 基准 |
| 1000 | $29.99 | $3.00 | 40% |
| 5000 | $99.99 | $2.00 | 60% |
| 20000 | $299.99 | $1.50 | 70% |

生成数量按图片 8–40、视频 24–32 积分区间动态估算。例如 1000 积分显示约 25–125 张图片、31–41 个视频；最终仍以提交前实际价格为准。

## 5. 优惠规则

- 名称：首次创作者加赠
- 规则：额外 60%
- 套餐：1000 积分创作者包
- 到账：1600 积分
- 价格：$29.99
- 优惠码：`WELCOME60`
- 延迟：45 秒
- 有效期：30 分钟
- 关闭限频：24 小时
- 生命周期：保存在当前设备，刷新不重置
- 服务端 metadata：
  - `package_id`
  - `base_credits`
  - `granted_credits`
  - `offer_code`
  - `offer_extra_percent`
  - `pricing_version`

## 6. 支付安全状态

### 已完成

- 普通用户只看到服务端确认已配置的支付方式。
- 测试/沙盒标记只向管理员显示。
- 前端不再调用演示购买或本地伪造到账。
- 前端不再传递可信价格与到账积分。
- 订单创建使用幂等键。
- Stripe Webhook 验签后发放积分。
- 支付成功返回只显示“等待服务端确认”，不直接到账。
- 支付取消明确显示未发放积分。

### 当前保持关闭

- PayPal：虽然保留服务端创建订单能力，但缺少完整的 PayPal 验签捕获 Webhook，因此对消费者隐藏。
- Stripe：生产配置未确认完整时页面显示“支付暂未开放”。

## 7. 验证结果

### 自动验证

- `npm run typecheck`：通过。
- `npm run build`：通过。
- `node --test tests/pricing-config.test.js`：4/4 通过。
- `git diff --check`：通过。

### 浏览器验证

本地地址：`http://localhost:5173/pricing.html`

- 页面标题与 UTF-8 中文：通过。
- 四档套餐：通过。
- 推荐套餐：1000 积分，正确显示“最受欢迎”。
- 单位价与真实节省比例：通过。
- 动态图片/视频数量：通过。
- 未登录余额：显示“登录后查看当前余额”。
- 未配置支付方式：不显示支付方式，四个按钮全部禁用。
- 普通用户不显示测试环境标记：通过。
- 横向溢出检查：未发现。
- Console error / warning：0。
- 优惠数学单元测试：1000 → 1600，通过。
- 优惠过期与刷新逻辑：代码与纯函数检查通过；未等待 45 秒执行真实交互。
- 支付取消/成功：代码路径与服务端状态映射检查通过；未触发真实付款。

### 截图

- 截图路径：未生成。
- 原因：内置浏览器页面截图接口连续超时；为避免把应用窗口截图误当作页面截图，未保留错误文件。
- DOM 页面结构、按钮禁用状态、无横向溢出和控制台日志均已完成浏览器检查。

## 8. 未完成项与风险

1. 数据库迁移尚未应用。
   - 必须先审核并应用 `202607260002_consumer_credit_checkout.sql`，再部署更新后的 Edge Functions。
2. Edge Functions 尚未部署。
   - 当前本地前端会正确显示“支付暂未开放”。
3. Stripe 真实测试付款未执行。
   - 按任务要求没有自动执行真实付款。
4. PayPal Webhook 未实现。
   - 因此 PayPal 保持隐藏，不能标记为已接入。
5. 生产 `consumer_credit_pricing` 尚未发布。
   - 当前使用统一默认配置；后续可在 `site_settings` 发布相同结构，无需改页面。
6. 项目原有构建警告仍存在。
   - 非模块脚本标签与部分首页资源路径警告不是本阶段引入，构建仍成功。
7. `app.js` 内仍保留旧演示结账函数作为不可达兼容代码。
   - 当前页面不再绑定或调用这些函数；后续可在独立清理任务中删除，避免影响仍引用旧接口的历史页面。

## 9. 上线顺序

1. 审核并应用新增数据库迁移。
2. 配置 Stripe 测试密钥、Webhook Secret、功能开关和回调地址。
3. 部署 `ai` 与 `billing` Edge Functions。
4. 在 Stripe 测试环境完成一次成功、一次取消和一次重复 Webhook 测试。
5. 确认订单、积分交易和余额一致后再开放消费者购买按钮。
6. PayPal 必须先完成验签 Webhook，再把 provider 状态改为可用。
